---
import {
	Image as AstroImage,
	type LocalImageProps,
	type RemoteImageProps,
} from "astro:assets";
const props = Astro.props as LocalImageProps | RemoteImageProps;
---

<AstroImage {...props} />
